"""physgate: a deterministic harness for agentic mechatronic design.

This package is deliberately empty. Each subpackage is created by the work that
first needs it, so that an import path never exists before the thing behind it
does.
"""

from __future__ import annotations

__version__ = "0.0.0"
