"""Root of the test tree.

No fixtures are declared yet. This file exists so that the directory is the
anchor pytest resolves test module names against, and so the first spec that
needs a shared fixture has an obvious place to put it rather than inventing
one.
"""

from __future__ import annotations
